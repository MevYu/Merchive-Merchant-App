# @merchive/* is hosted on GitHub Packages, not public npm.
# Auth: export NODE_AUTH_TOKEN=<a GitHub PAT with read:packages> before install
# (CI: ${{ secrets.GITHUB_TOKEN }}). The token is NOT committed.
@merchive:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
